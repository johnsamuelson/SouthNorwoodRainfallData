name: Update rainfall data

on:
  schedule:
    # Every hour, at minute 5. GitHub may delay scheduled runs slightly at busy times.
    - cron: "5 * * * *"
  workflow_dispatch: {}   # lets you trigger it manually from the Actions tab

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run update script
        run: python scripts/update_data.py

      - name: Commit and push changes
        run: |
          git config user.name "rainfall-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/rainfall.db docs/data/*.json
          if git diff --cached --quiet; then
            echo "Nothing changed."
          else
            git commit -m "Update rainfall data"
            git push
          fi
